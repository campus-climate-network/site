// Canonical origin for all absolute URLs (sitemap, metadata, JSON-LD).
// The apex domain 307-redirects to www, so www is the canonical host.
export const SITE_URL = 'https://www.campusclimatenetwork.org'
